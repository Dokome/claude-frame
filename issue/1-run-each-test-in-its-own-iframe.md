---
title: "Run each test in its own <iframe>"
url: "https://github.com/facebook/react/pull/1"
author: benjamn
date: 2013-05-29
type: pull_request
state: merged
labels: []
assignees: []
---
# Run each test in its own <iframe>

This is not blocking the initial launch, so feel free to put it on the back-burner for now.

The Jasmine test harness still runs in the parent window and reports to PhantomJS via `window.callPhantom`, but each test `<iframe>` has its own copy of `react-test.js` and each individual test module is required in the global context of a separate `<iframe>`.

This gives us a significant approximation of the benefits of mocking, at least in terms of isolating tests from one another.

cr @jeffmo @zpao


---

## Comments

### @zpao (2013-06-03)

A++, merge away when you're ready


---

### @petehunt (2013-06-03)

I like this much better than our current test system.

But isn't it masking the fact that dumpCache() doesn't work consistently between our phantom and jstest test runners? I think that's why we had the original issue where the reactRoot count was not reset when running in phantom. Doesn't that mean that some tests will pass in phantom (because they're better isolated) and fail in jstest?


---

### @benjamn (2013-06-03)

@petehunt good question. I believe jstest actually creates a new window environment for each test, which would mean that the `<iframe>` strategy is as close as we can get in a real browser: https://phabricator.fb.com/diffusion/E/browse/tfb/trunk/www/scripts/third_party/jstest/runner/run-single-jstest.js

So I think/hope that we will have fewer disagreements between phantom and jstest.

It's still masking the fact that `dumpCache` doesn't work the same way, yes. But making `dumpCache` work would require rewriting the module system that Browserify uses. That's not out of the question by any means, but right now it's nice to be testing the same imperfect module system that we ship in production Browserify bundles.

cc @jeffmo who might know better


---

### @petehunt (2013-06-03)

:+1: 


---

### @release-fk (2025-06-23)


<!--
  0 failure: 
  1 warning:  Coverage data had...
  1 messages
  
  DangerID: danger-id-Danger;
-->


<table>
  <thead>
    <tr>
      <th width="50"></th>
      <th width="100%" data-danger-table="true">Warnings</th>
    </tr>
  </thead>
  <tbody><tr>
      <td>:warning:</td>
      <td>Coverage data had invalid formatting at path '/Users/karthick.s/Documents/cloudqwest/tracking-frontend-client/coverage/coverage-final.json'</td>
    </tr>
  </tbody>
</table>


<table>
  <thead>
    <tr>
      <th width="50"></th>
      <th width="100%" data-danger-table="true">Messages</th>
    </tr>
  </thead>
  <tbody><tr>
      <td>:book:</td>
      <td>No TypeScript/JavaScript files were modified in the src directory.</td>
    </tr>
  </tbody>
</table>


<p align="right">
  Generated by :no_entry_sign: <a href="https://danger.systems/js">dangerJS</a> against 603c9ef6a8d70d3cf29ee9d0a9d7969abce48ac4
</p>


---

### @robin-park-querypie (2026-01-27)

Test comment from Round 2

---

### @Serhan-Asad (2026-01-27)

<!-- PDD_WORKFLOW_STATE:e2e_fix:issue-1
{
  "workflow": "e2e_fix",
  "issue_url": "https://github.com/facebook/react/issues/1",
  "issue_number": 1,
  "current_cycle": 1,
  "last_completed_step": 1,
  "step_outputs": {
    "1": "I've completed the analysis and posted my findings to the GitHub issue. \n\n## Summary\n\nAfter thoroughly analyzing GitHub issue #1 from facebook/react, I've determined that:\n\n1. **No Unit Tests Identified**: This issue is a historical React PR from 2013 about test infrastructure (running Jasmine tests in iframes), not a `pdd bug` generated issue with executable unit tests.\n\n2. **Missing Expected Content**: The issue lacks:\n   - Unit test file paths (like `tests/test_*.py`)\n   - `FILES_CREATED:` sections\n   - Python test code blocks\n   - Dev units to fix with `pdd fix`\n\n3. **Status Posted**: I've commented on the issue with a detailed report following the required format, explaining that no unit tests were found and therefore no `pdd fix` runs were needed.\n\nThe issue appears to be a test case for the workflow system itself, but it doesn't contain the actual unit test content that would be generated by `pdd bug`. For Step 1 to work properly, the issue would need to contain explicit test file paths and associated dev units that can be tested and fixed.",
    "2": "FAILED: All agent providers failed",
    "3": "FAILED: All agent providers failed",
    "4": "FAILED: All agent providers failed",
    "5": "FAILED: All agent providers failed",
    "6": "FAILED: All agent providers failed"
  },
  "dev_unit_states": {},
  "total_cost": 0.16977214999999998,
  "model_used": "anthropic",
  "changed_files": [],
  "last_saved_at": "2026-01-27T14:14:14.028869",
  "github_comment_id": 3806658996
}
-->

---

---

## Commits

| SHA | Author | Message | Date |
|-----|--------|---------|------|
| `83029eb75600e306696bc5bf03c6d899fa3c10f8` | @benjamn | Make the bin/jsx source and output directories configurable. | 2013-05-28 |
| `f8af93237adaa7c02df9edcbfccd07e6fdaaa0ed` | @benjamn | Use bin/jsx and browserify to build a jasmine bundle. | 2013-05-28 |
| `03f92bb15573e380cdf7261e3f952b8792e4cd17` | @benjamn | Polyfill Function.prototype.bind during tests. | 2013-05-28 |
| `c740373b311a2aa43a512f1bf53e1de72635c02a` | @benjamn | Fix some silly uses of Function.prototype.bind in jasmine-support.js. | 2013-05-28 |
| `009c0b92002fa1aeae985c1a5c8707119a945647` | @benjamn | Expose test modules for requirement. | 2013-05-28 |
| `42f8d155f8f240c457c969ac683d8b25876708ee` | @benjamn | Silence tests unsupported in PhantomJS.

These tests can still be run in the browser using `grunt test --debug`. | 2013-05-28 |
| `6cfa71a3c2b431fd2dfbb78379c795b8aeb2ee53` | @benjamn | Run each test in an <iframe>. | 2013-05-28 |
| `906b8f3f95c77482e432fec84f21c50381a68cf1` | @benjamn | Create testing <iframe>s dynamically, according to grunt config. | 2013-05-29 |
| `603c9ef6a8d70d3cf29ee9d0a9d7969abce48ac4` | @benjamn | Implement constructor-aware binding per @zpao's request.

When the function to be bound does not have a prototype, ignore the constructor case. | 2013-06-03 |

---

## Changed Files

| File | Status | Additions | Deletions |
|------|--------|-----------|-----------|
| `Gruntfile.js` | modified | +2 | -0 |
| `grunt/config/browserify.js` | modified | +15 | -1 |
| `grunt/config/jsx/jsx.js` | modified | +19 | -3 |
| `grunt/config/phantom.js` | modified | +4 | -1 |
| `grunt/tasks/browserify.js` | modified | +14 | -4 |
| `grunt/tasks/jsx.js` | modified | +2 | -2 |
| `grunt/tasks/phantom.js` | modified | +8 | -0 |
| `src/test/all.js` | modified | +48 | -9 |
| `src/utils/__tests__/ImmutableObject-test.js` | modified | +4 | -0 |
| `test/frame.html` | added | +18 | -0 |
| `test/index.html` | modified | +9 | -3 |
| `test/phantom-harness.js` | modified | +33 | -11 |
| `vendor/jasmine/HtmlReporter.js` | renamed | +2 | -0 |
| `vendor/jasmine/PrintReporter.js` | renamed | +2 | -1 |
| `vendor/jasmine/ReporterView.js` | renamed | +2 | -0 |
| `vendor/jasmine/SpecView.js` | renamed | +2 | -0 |
| `vendor/jasmine/SuiteView.js` | renamed | +2 | -0 |
| `vendor/jasmine/all.js` | added | +38 | -0 |
| `vendor/jasmine/console.js` | renamed | +0 | -0 |
| `vendor/jasmine/diff.js` | renamed | +0 | -0 |
| `vendor/jasmine/jasmine-html.js` | modified | +2 | -0 |
| `vendor/jasmine/jasmine-support.js` | renamed | +3 | -10 |
| `vendor/jasmine/jasmine.js` | modified | +12 | -14 |
| `vendor/jasmine/phantom.js` | renamed | +0 | -20 |

---

## Review Comments

### @zpao on `src/test/all.js` (2013-06-03)

> ```
> @@ -2,17 +2,29 @@
 // modules in src/test and to specify an ordering on those modules, since
 // some still have implicit dependencies on others.
 
-require("./phantom");
-require("./console");
+var Ap = Array.prototype;
+var slice = Ap.slice;
+var Fp = Function.prototype;
+
+if (!Fp.bind) {
+  // PhantomJS doesn't support Function.prototype.bind natively, so
+  // polyfill it whenever this module is required.
+  Fp.bind = function(context) {
+    var func = this;
+    var args = slice.call(arguments, 1);
+    return args.length > 0 ? function() {
> ```

ternary across this many lines is _groosssss_. Can you just use `if`?

What about just using the shim @ https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/bind - this here is not quite the same.


### @zpao on `src/utils/__tests__/ImmutableObject-test.js:103` (2013-06-03)

> ```
> @@ -100,6 +100,7 @@ describe('ImmutableObject', function() {
   });
 
   testDev('should prevent shallow field addition when strict', function() {
+    if (window.callPhantom) return;
> ```

These become no-ops internally, yea?


### @benjamn on `src/utils/__tests__/ImmutableObject-test.js:103` (2013-06-03)

> ```
> @@ -100,6 +100,7 @@ describe('ImmutableObject', function() {
   });
 
   testDev('should prevent shallow field addition when strict', function() {
+    if (window.callPhantom) return;
> ```

The tests will pass, but there will be fewer `expect`-ations counted in the final tally.

These tests can still be run in the browser if you do `grunt test --debug`.


### @benjamn on `src/test/all.js` (2013-06-03)

> ```
> @@ -2,17 +2,29 @@
 // modules in src/test and to specify an ordering on those modules, since
 // some still have implicit dependencies on others.
 
-require("./phantom");
-require("./console");
+var Ap = Array.prototype;
+var slice = Ap.slice;
+var Fp = Function.prototype;
+
+if (!Fp.bind) {
+  // PhantomJS doesn't support Function.prototype.bind natively, so
+  // polyfill it whenever this module is required.
+  Fp.bind = function(context) {
+    var func = this;
+    var args = slice.call(arguments, 1);
+    return args.length > 0 ? function() {
> ```

Sure, sure.

For what it's worth, the `static_upstream` polyfill doesn't bother with constructor function binding, either. Seems like a nontrivial cost to pay for a minority use case.

